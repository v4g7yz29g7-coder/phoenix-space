function приветствиеИзФеникса() {
  return 'Привет из Феникса';
}

// Пример использования:
console.log(приветствиеИзФеникса()); // Выведет: Привет из Феникса
// В роутере или контроллере
app.get('/api/hello', (req, res) => {
  const message = приветствиеИзФеникса();
  res.json({ message });
});
